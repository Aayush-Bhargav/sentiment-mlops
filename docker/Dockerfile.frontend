# 1. Start with Python
FROM python:3.9-slim

WORKDIR /app

# 2. Install libraries (this layer is cached)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy the Streamlit application entry point
COPY frontend.py .

# 4. Open the door (Port 8501 is standard for Streamlit)
EXPOSE 8501

# 5. Run Streamlit
CMD ["streamlit", "run", "frontend.py", "--server.port=8501", "--server.address=0.0.0.0"]